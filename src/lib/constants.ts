/**
 * Site-wide constants shared across route handlers.
 * Previously the production site URL was copy-pasted into 6+ route files;
 * centralising it here removes the drift risk (R6).
 */
export const SITE_URL = "https://registrum.co.uk";
